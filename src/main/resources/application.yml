server:
  port: 8080

spring:
  datasource:
    url: jdbc:mariadb://34.64.84.117:3306/switchmanga?useUnicode=true&characterEncoding=utf8mb4
    username: switchmanga
    password: 2025
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

logging:
  level:
    com.switchmanga.api: DEBUG
    org.hibernate.SQL: DEBUG